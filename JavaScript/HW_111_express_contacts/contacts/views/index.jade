extends layout

block content
  h1= title
  p Welcome to #{title}

  if allContacts
    each contact in allContacts
      h2 Id: #{contact.id}
      h2 First: #{contact.first}
      h2 Last: #{contact.last}
